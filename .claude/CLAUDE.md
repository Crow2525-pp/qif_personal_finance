# Claude Context Pointer

Claude Code should load the repo-root `CLAUDE.md` as the source of truth.

This file is retained only for users or tools that look inside `.claude/`.
Do not duplicate project instructions here; duplication causes stale guidance and wastes context.
